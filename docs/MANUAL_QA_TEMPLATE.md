# 上线前手工验收模板

用途：把“上线前手工验收”从一段 Markdown 列表，变成可以真正执行、记录结果、交接给别人继续跑的模板。

本文件是模板，不是结果。每次准备发布、切换配置或做大改动前，优先运行：

```bash
npm run qa:init -- --env staging
```

脚本会自动生成一份实际验收记录，你再在生成文件里填写结果。

生成的记录顶部会包含机器可读标记：

```text
<!-- QA_AUDIT_STATUS: pending -->
```

验收全部通过后，把它改成：

```text
<!-- QA_AUDIT_STATUS: passed -->
```

如果有阻断项，改成 `blocked`；如果本轮作废，改成 `failed`。`npm run completion:audit` 会复用 `qa:doctor` 的判断：只有状态为 `passed`、没有未勾选项目、没有空白字段的记录，才会被当作可用完成证据。

提交前可以运行：

```bash
npm run qa:doctor
```

它会检查最新手工 QA 记录的状态标记、未勾选项目和空白字段，避免未完成记录被误用作完成证据。

如果要复核某一份旧记录，可以指定文件：

```bash
npm run qa:doctor -- --file docs/manual-qa-runs/YYYY-MM-DD-<环境名>-manual-qa.md
```

建议生成或复制到：

```text
docs/manual-qa-runs/YYYY-MM-DD-<环境名>-manual-qa.md
```

例如：

```text
docs/manual-qa-runs/2026-05-25-staging-manual-qa.md
```

可选地，你也可以追加短标识区分不同轮次：

```bash
npm run qa:init -- --env production --slug prelaunch
```

---

## 1. 执行信息

- 执行日期：
- 执行环境：`local / staging / production`
- 执行人：
- 代码版本 / commit：
- 站点域名：
- Supabase project ref：
- 是否使用 `VITE_SHARE_LINK_MODE=preview`：
- 管理员测试账号：
- 普通用户测试账号：

## 2. 自动化前置结果

先填自动化结果，再开始手工走页面。

- `npm run verify`：
  - [ ] 通过
  - 备注：

- `npm run launch:doctor`：
  - [ ] 已执行
  - 结果摘要：

- `npm run smoke:doctor`：
  - [ ] 已执行
  - 结果摘要：

- `npm run smoke`：
  - [ ] 通过
  - 备注：

- 验收记录文件：
  - 路径：
  - 是否已提交到仓库：

## 3. 访客路径

### 3.1 首页与画廊

- [ ] 首页正常打开
- [ ] 背景图、头部、精选区、记忆碎片区正常显示
- [ ] 搜索、时间筛选、标签筛选可用
- [ ] 刷新空间按钮正常
- [ ] 空筛选状态文案正常

证据：
- 截图：
- 备注：

### 3.2 大图查看器

- [ ] 点击卡片可打开大图
- [ ] 关闭按钮可用
- [ ] 上一张 / 下一张可用
- [ ] `下载原图` 可用
- [ ] `复制分享链接` 可用
- [ ] `举报图片` 按钮可见

证据：
- 截图：
- 备注：

### 3.3 举报限制

- [ ] 首次举报成功
- [ ] 同图重复待处理举报被拦截
- [ ] 短时间连续举报被限频
- [ ] 错误提示可理解

证据：
- 截图 / 返回信息：
- 备注：

## 4. 普通用户路径

### 4.1 认证

- [ ] 登录成功
- [ ] 若开启公开注册：注册成功
- [ ] 若关闭公开注册：注册入口已收起或明确提示关闭
- [ ] 重发确认邮件可触发
- [ ] 忘记密码可触发

证据：
- 截图：
- 备注：

### 4.2 上传与个人中心

- [ ] 批量上传弹窗可打开
- [ ] 上传限制文案正确
- [ ] 上传成功后首页状态条正常
- [ ] `我的图片` 可切换到当前用户范围
- [ ] `我的空间` 正常显示统计、范围和下一步卡片

证据：
- 截图：
- 备注：

### 4.3 内容维护

- [ ] 用户可编辑自己的标题 / 描述 / 标签
- [ ] 用户可删除自己的图片
- [ ] 用户不能编辑别人的图片
- [ ] 命中上传上限或被暂停上传时，提示明确且后端真正拒绝

证据：
- 截图 / 返回信息：
- 备注：

## 5. 管理员路径

### 5.1 运维概览

- [ ] `运维概览` 可打开
- [ ] 指标卡片显示正常
- [ ] 运行监控、健康检查、注册与邀请信号、上线准备、数据留存与清理都可见
- [ ] 最近上传、最近操作可查看

证据：
- 截图：
- 备注：

### 5.2 图片管理

- [ ] 搜索可用
- [ ] 状态筛选可用
- [ ] 排序可用
- [ ] 批量选择可用
- [ ] 批量 ZIP 可用
- [ ] 批量删除可用
- [ ] 回填大小可用
- [ ] 设置精选可用
- [ ] 下架 / 恢复公开可用

证据：
- 截图：
- 备注：

### 5.3 用户管理

- [ ] 用户列表可加载
- [ ] 设置 / 取消管理员可用
- [ ] 暂停 / 恢复上传可用
- [ ] 设置上传总上限可用
- [ ] 邀请用户可用
- [ ] 邀请策略与已用额度显示正常

证据：
- 截图：
- 备注：

### 5.4 举报与审计

- [ ] 举报列表可加载
- [ ] 已处理 / 驳回 / 重新打开可用
- [ ] 操作日志可见对应记录

证据：
- 截图：
- 备注：

### 5.5 站点设置

- [ ] 标签、上传上限、批量张数可修改
- [ ] 上传时间窗口可修改
- [ ] 邀请时间窗口可修改
- [ ] `普通用户上传` 开关可修改
- [ ] `公开注册` 开关可修改
- [ ] `上线准备`、`导出与备份策略`、`运行监控口径`、`数据留存与清理策略`、`反滥用与频率限制口径` 都可见

证据：
- 截图：
- 备注：

## 6. 手机端路径

- [ ] 顶部按钮区域可横向滚动或完整可点击
- [ ] 画廊筛选栏在窄屏下可用
- [ ] 大图查看器按钮没有被地址栏遮挡
- [ ] 上传弹窗内容可滚动
- [ ] 管理后台页签、统计、表格在窄屏下仍能操作

设备 / 浏览器：
- iPhone / Android：
- 浏览器：

证据：
- 截图：
- 备注：

## 7. 分享预览专项

- [ ] 主站分享链接打开正常
- [ ] 若启用 preview：`share-memory` 链接可打开
- [ ] 标题 / 描述 / 缩略图符合预期
- [ ] 若有社交抓取调试器，已额外核对

证据：
- 链接：
- 调试器结果：
- 备注：

## 8. 阻断项汇总

### 阻断上线

- [ ] 无
- 问题：
- 影响范围：
- 需要谁处理：

### 可带着上线的已知问题

- [ ] 无
- 问题：
- 临时绕行：
- 计划修复时间：

## 9. 最终结论

- [ ] 可以继续推进上线
- [ ] 可以继续推进下一轮配置，但暂不上线
- [ ] 需要修复阻断项后重跑

结论说明：

---

## 10. 附加证据

- 截图目录：
- 录屏：
- 相关日志：
- 相关 `doctor` 输出：
