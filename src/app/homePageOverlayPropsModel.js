export function buildHomeOverlayProps({
  uploadModal,
  userPanel,
  authModal,
  themeModal,
  adminPanel,
  viewer,
  blogEditor,
}) {
  return {
    uploadModal,
    userPanel,
    authModal,
    themeModal,
    adminPanel,
    viewer,
    blogEditor,
  };
}
